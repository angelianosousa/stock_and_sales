<%= form_with(model: [:users_backoffice, product], local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :price_monetary %>
    <%= form.text_field :price_monetary %>
  </div>

  <div class="field">
    <%= form.label :in_stock %>
    <%= form.number_field :in_stock %>
  </div>

  <div class="field">
    <%= form.label :safety_margin %>
    <%= form.number_field :safety_margin %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
